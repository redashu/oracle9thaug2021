apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: ashusvc1
  name: ashusvc1 # name of service 
spec:
  ports:
  - name: firstc1
    port: 1234 # port of service 
    protocol: TCP
    targetPort: 80 # pod app port 
  - name: second2
    port: 1235 # port of service 
    protocol: TCP
    targetPort: 8080 # pod app port
  selector: # its a pod finder 
    x: helloashuapp # changed label of POD here 
  type: NodePort # type of service 
status:
  loadBalancer: {}
